@model TarefaViewModel // informa que tipo vou receber

@{
    ViewData["Title"] = "Tarefas";
}

<div class="panel panel-default tarefa-panel">
    

    <table class="table table=hover">
        <thead>
            <tr>
                <td>&#x2714;</td>
                <td>Tarefa</td>
                <td>Previsão</td>
            </tr>
        </thead>
        @foreach(var item in Model.TarefaItems){
            <tr>
                <td>
                    <input type="checkbox" name="@item.ID" value="true"  class="done-checkbox">
                    <td>@item.Nome</td>
                    <td>@item.DataConclusao</td>
                </td>
                
            </tr>
        }
    </table>
    <div class="panel-footer add-item-form">
        <form>
            <div id="add-item-error" class="text-danger"></div>
                <label for="add-item-title">Incluir nova tarefa</label>
                <input id="add-item-litle">
                <button type="button" id="add-item-button">Incluir</button>
        </form>
    </div>
</div>
